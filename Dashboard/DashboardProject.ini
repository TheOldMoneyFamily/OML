/DashboardProject
├── app
│   ├── __init__.py
│   ├── main.py               # FastAPI entry point
│   ├── models.py             # SQLAlchemy models for Users, Roles, Permissions, Courses, Subscriptions, Tasks
│   ├── schemas.py            # Pydantic models
│   ├── crud.py               # CRUD helper functions
│   ├── dependencies.py       # Database session and auth/role dependencies
│   ├── auth.py               # JWT authentication and token endpoint
│   ├── pdf_generator.py      # Generates a PDF outlining the dashboard plan
│   └── routes
│       ├── admin.py          # Admin-specific endpoints (user management, etc.)
│       ├── dashboard.py      # Common dashboard endpoints (home, profile, etc.)
│       └── teacher.py        # Teacher-specific endpoints (course management sample)
├── config.py                 # Application settings (loads from .env)
├── requirements.txt          # All required dependencies
└── .env                      # Environment variables (not committed to VCS)
